@model List<JewelryShop.Model.DanhMuc>

<div class="nav navbar-left">
    <div class="container">
        <div class="navbar navbar-fixed-bottom">
            <div class="collapse navbar-collapse">
                <ul class="nav navbar">
                    @foreach (var item in Model)
                    {
                        <li><a href="@Url.Action("Index","SanPham",new { id=item.DanhMucID})">@item.TenDanhMuc</a></li>
                    }
                </ul>
            </div>
        </div>
    </div>
</div>